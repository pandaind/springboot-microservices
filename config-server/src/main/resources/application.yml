server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations

---
spring:
  config:
    activate:
      on-profile: "docker"
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
